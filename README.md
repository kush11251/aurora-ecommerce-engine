# Aurora Ecommerce
A scalable e-commerce platform with payment gateway integration

## Getting Started
1. Clone the repository: `git clone https://github.com/user/aurora-ecommerce-engine.git`
2. Navigate to the project directory: `cd aurora-ecommerce-engine`
3. Install dependencies: `npm install`
4. Run the application: `npm start`